name: Archived Storefront v1.4 Check

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived legacy storefront v1.4 gate. Production uses the clean Storefront V1 baseline and current Browser E2E checks."
