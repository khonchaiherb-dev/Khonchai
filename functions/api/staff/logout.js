import {json,sameOrigin,getStaffSession,securityEvent} from '../../_lib/staff-auth.js';
export async function onRequestPost({request,env}){if(!sameOrigin(request))return json({error:'invalid_origin'},403);const s=await getStaffSession(request,env,{touch:false});if(s){await env.DB.prepare("UPDATE staff_sessions SET revoked_at=datetime('now') WHERE token_hash=?").bind(s.tokenHash).run();await securityEvent(env,{staffUserId:s.id,eventType:'staff_logout',route:'/api/staff/logout',method:'POST',statusCode:200})}return json({ok:true},200,{'Set-Cookie':'kch_staff=; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=0'})}
