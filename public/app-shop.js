function chrome(body,active=current){return `<div class="shell"><header class="topbar"><div class="brand" data-go="home"><span class="mark">K</span><div><b>KHONCHAIHERB</b><small>PREMIUM THAI HERBAL</small></div></div><div class="actions"><button class="icon-btn" data-focus-search aria-label="ค้นหา">${I('search')}</button><button class="icon-btn" data-go="orders" aria-label="คำสั่งซื้อ">${I('local_shipping')}</button><button class="icon-btn" data-go="cart" aria-label="ตะกร้า">${I('shopping_cart')}<i class="badge">${count()}</i></button></div></header>${body}<nav class="bottom"><button data-go="home" class="${active==='home'?'active':''}">${I('home')}หน้าหลัก</button><button data-go="orders" class="${active==='orders'?'active':''}">${I('local_shipping')}คำสั่งซื้อ</button><button data-go="cart" class="${active==='cart'?'active':''}">${I('shopping_bag')}ตะกร้า</button><button data-go="account" class="${active==='account'?'active':''}">${I('person')}ฉัน</button></nav></div>`}
function card(p){const off=p.old>p.price?Math.round((1-p.price/p.old)*100):0;return `<article class="card" data-product="${esc(p.slug)}">${visual(p)}<div class="card-info"><h3>${esc(p.name)}</h3><div class="rating">${I('star')} ${p.rating.toFixed(1)} <span>ขายแล้ว ${p.sold.toLocaleString('th-TH')}</span></div><div class="price"><b>${M(p.price)}</b>${off?`<small>-${off}%</small>`:''}</div><div class="card-foot"><span>${p.stock>0?'พร้อมส่งจากร้านทางการ':'สินค้าหมดชั่วคราว'}</span><button class="round-add" data-add="${p.id}" ${p.stock<=0?'disabled':''}>${I('add')}</button></div></div></article>`}
function couponCard(c){const saved=claimed.includes(c.code);return `<div class="coupon"><span>${I('confirmation_number')}</span><div><b>${esc(c.label||c.code)}</b><span>ขั้นต่ำ ${M(c.min_spend||0)} • โค้ด ${esc(c.code)}</span></div><button data-claim="${esc(c.code)}" ${saved?'disabled':''}>${saved?'เก็บแล้ว':'เก็บ'}</button></div>`}
function home(){current='home';const list=PRODUCTS.filter(p=>activeCategory==='ทั้งหมด'||p.cat===activeCategory);app.innerHTML=chrome(`<div class="search">${I('search')}<input id="shop-search" placeholder="ค้นหาสมุนไพร สินค้าขายดี..." aria-label="ค้นหาสินค้า"><button id="search-btn">ค้นหา</button></div><section class="hero"><div class="hero-copy"><span class="eyebrow">KHONCHAIHERB SELECTED</span><h1>สมุนไพรไทย<br>ช้อปง่ายในไม่กี่ขั้นตอน</h1><p>คัดสรร • สั่งง่าย • ติดตามชัด • บริการตรงจากแบรนด์</p><button class="primary" data-scroll-products>เลือกซื้อสินค้า</button></div><div class="hero-art">🌿</div></section><section class="trust"><div>${I('local_shipping')}<span><b>ส่งไว</b><small>ติดตามง่าย</small></span></div><div>${I('payments')}<span><b>COD</b><small>เก็บปลายทาง</small></span></div><div>${I('verified')}<span><b>ซื้อมั่นใจ</b><small>ร้านทางการ</small></span></div></section><section class="section no-right"><div class="head"><div><span class="flash-title">FLASH DEAL</span><div class="timer">${I('schedule')} เหลือเวลา <strong id="flash-timer">--:--:--</strong></div></div><button class="link" data-scroll-products>ดูทั้งหมด ›</button></div><div class="hscroll">${PRODUCTS.filter(p=>p.stock>0).slice(0,4).map(p=>`<div class="flash-item" data-product="${esc(p.slug)}">${visual(p)}<b>${M(p.price)}</b><del>${M(p.old)}</del><small>ขายแล้ว ${p.sold.toLocaleString('th-TH')}</small></div>`).join('')}</div></section><div id="coupon-strip">${COUPONS.slice(0,2).map(couponCard).join('')}</div><div class="cats"><button data-cat="ทั้งหมด" class="${activeCategory==='ทั้งหมด'?'active':''}">✨<span>ทั้งหมด</span></button><button data-cat="ชาสมุนไพร" class="${activeCategory==='ชาสมุนไพร'?'active':''}">🌿<span>ชาสมุนไพร</span></button><button data-cat="ดูแลร่างกาย" class="${activeCategory==='ดูแลร่างกาย'?'active':''}">🍃<span>ดูแลร่างกาย</span></button><button data-cat="เครื่องดื่ม" class="${activeCategory==='เครื่องดื่ม'?'active':''}">🫖<span>เครื่องดื่ม</span></button><button data-cat="ชุดของขวัญ" class="${activeCategory==='ชุดของขวัญ'?'active':''}">🎁<span>ชุดของขวัญ</span></button></div><section class="section" id="recommend"><div class="head"><div><h2>สินค้าแนะนำสำหรับคุณ</h2><div class="sub">เลือกจากสินค้าที่ได้รับความนิยม</div></div>${I('auto_awesome')}</div><div class="grid" id="product-grid">${list.length?list.map(card).join(''):`<div class="empty search-empty"><h2>ยังไม่มีสินค้าในหมวดนี้</h2></div>`}</div></section><section class="review-promo"><div class="stars">★★★★★</div><h3>รีวิวจากผู้ซื้อจริงช่วยให้เลือกง่ายขึ้น</h3><p>ระบบรองรับ Verified Purchase และคูปองตอบแทนหลังรีวิว</p></section>`);bind();startFlashTimer()}
function startFlashTimer(){const el=document.querySelector('#flash-timer');if(!el)return;const tick=()=>{const now=new Date(),end=new Date(now);end.setHours(23,59,59,999);let s=Math.max(0,Math.floor((end-now)/1000));const h=String(Math.floor(s/3600)).padStart(2,'0');s%=3600;const m=String(Math.floor(s/60)).padStart(2,'0'),sec=String(s%60).padStart(2,'0');el.textContent=`${h}:${m}:${sec}`};tick();clearInterval(window.__kchTimer);window.__kchTimer=setInterval(tick,1000)}
function product(p){current='product';selected=p;qty=1;const off=p.old>p.price?Math.round((1-p.price/p.old)*100):0;app.innerHTML=chrome(`<button class="back" data-go="home" aria-label="กลับ">${I('arrow_back')}</button>${visual(p,'large')}<section class="detail"><div class="pprice"><b>${M(p.price)}</b>${p.old>p.price?`<del>${M(p.old)}</del><span>ลด ${off}%</span>`:''}</div><h1>${esc(p.name)}</h1><p>${esc(p.desc)}</p><div class="stats"><span>⭐ ${p.rating.toFixed(1)}</span><span>ขายแล้ว ${p.sold.toLocaleString('th-TH')}</span><span>คงเหลือ ${p.stock}</span></div></section><div class="deal">${I('sell')}<div><b>ดีลจากร้าน</b><span>${COUPONS.map(c=>c.code).slice(0,2).join(' • ')} • ส่งฟรีตามเงื่อนไข</span></div>${I('chevron_right')}</div><div class="ship">${I('local_shipping')}<div><b>การจัดส่ง</b><span>คำนวณตามยอดคำสั่งซื้อ • รองรับเก็บเงินปลายทาง</span></div></div><section class="review"><div class="head"><h3>คะแนนและรีวิว</h3><b>${p.rating.toFixed(1)} / 5</b></div><div class="review-card"><b>ผู้ซื้อที่ยืนยันแล้ว</b> <span class="sub">Verified Purchase</span><div class="stars">★★★★★</div><p>แพ็กเรียบร้อย จัดส่งตามกำหนด และติดตามสถานะได้สะดวก</p></div></section><div class="buybar"><button class="ghost" data-favorite="${p.id}">${I('favorite')}<span>ถูกใจ</span></button><button class="ghost" data-go="home">${I('storefront')}<span>ร้านค้า</span></button><div class="qty"><button data-qty="-1">−</button><b id="qty">1</b><button data-qty="1">+</button></div><button class="add" data-add="${p.id}" ${p.stock<=0?'disabled':''}>เพิ่มลงตะกร้า</button><button class="buy" data-buy="${p.id}" ${p.stock<=0?'disabled':''}>${p.stock>0?'ซื้อเลย':'สินค้าหมด'}</button></div>`,'');bind()}
function add(id,q=1){const p=PRODUCTS.find(x=>x.id===Number(id));if(!p||p.stock<=0)return;const hit=cart.find(x=>x.id===p.id);const next=Math.min(p.stock,(hit?.qty||0)+q);if(hit)hit.qty=next;else cart.push({...p,qty:Math.min(q,p.stock)});save();document.querySelectorAll('.badge').forEach(x=>x.textContent=count());toast('เพิ่มลงตะกร้าแล้ว')}
function shippingInfo(sum){const threshold=699,fee=45,remaining=Math.max(0,threshold-sum);return {threshold,fee,remaining,ship:remaining===0?0:fee,progress:Math.min(100,Math.round(sum/threshold*100))}}
function cartPage(){current='cart';const sum=subtotal(),ship=shippingInfo(sum);app.innerHTML=chrome(`<main class="page"><h1 class="page-title">ตะกร้าสินค้า</h1>${cart.length?`<section class="shipping-progress"><div><b>${ship.remaining?`อีก ${M(ship.remaining)} รับสิทธิ์ส่งฟรี`:'คุณได้รับสิทธิ์ส่งฟรีแล้ว'}</b><small>ส่งฟรีเมื่อยอดสินค้าครบ ${M(ship.threshold)}</small></div><div class="progress"><i style="width:${ship.progress}%"></i></div></section>`:''}${cart.length?cart.map(x=>`<div class="cartrow">${visual(x)}<div class="grow"><b>${esc(x.name)}</b><small>${esc(x.cat)}</small><strong>${M(x.price)}</strong><div class="cart-controls"><div class="qty"><button data-cartqty="${x.id}:-1">−</button><b>${x.qty}</b><button data-cartqty="${x.id}:1">+</button></div><button class="remove" data-remove="${x.id}" aria-label="ลบสินค้า">${I('delete')}</button></div></div></div>`).join(''):`<div class="empty">${I('shopping_bag')}<h2>ยังไม่มีสินค้าในตะกร้า</h2><p>เลือกสินค้าแล้วกลับมาที่นี่ได้ทันที</p><button class="primary" data-go="home">เลือกซื้อสินค้า</button></div>`}${cart.length?`<section class="voucher-box"><div class="voucher-title">${I('confirmation_number')}<b>คูปองร้านค้า</b></div>${claimed.length?claimed.map(code=>{const c=COUPONS.find(x=>x.code===code)||{code,label:code,min_spend:0};return `<label class="voucher-choice"><input type="radio" name="coupon" value="${esc(code)}" ${activeCoupon===code?'checked':''}><span><b>${esc(c.label||code)}</b><small>${esc(code)} • ขั้นต่ำ ${M(c.min_spend||0)}</small></span></label>`}).join(''):`<p class="sub">ยังไม่มีคูปองที่เก็บไว้</p>`}${activeCoupon?`<button class="clear-coupon" data-clear-coupon>ไม่ใช้คูปอง</button>`:''}</section><div class="summary"><div><span>ยอดสินค้า</span><b>${M(sum)}</b></div><div><span>ค่าจัดส่งโดยประมาณ</span><b>${ship.ship?M(ship.ship):'ฟรี'}</b></div></div><div class="sticky-pay"><span>รวมโดยประมาณ <b>${M(sum+ship.ship)}</b></span><button class="primary" data-go="checkout">ชำระเงิน (${count()})</button></div>`:''}</main>`);bind()}
function previewDiscount(sum){const c=COUPONS.find(x=>x.code===activeCoupon);if(!c||sum<Number(c.min_spend||0))return 0;let d=c.type==='percent'?sum*(Number(c.value||0)/100):Number(c.value||0);if(c.max_discount!=null)d=Math.min(d,Number(c.max_discount));return Math.max(0,Math.min(d,sum))}
function checkout(){if(!cart.length){cartPage();return}current='checkout';checkoutKey=checkoutKey||uuid();const sub=subtotal(),disc=previewDiscount(sub),ship=shippingInfo(sub).ship,total=sub-disc+ship;app.innerHTML=chrome(`<main class="page"><h1 class="page-title">ชำระเงิน</h1><section class="address checkout-form"><h3>ข้อมูลจัดส่ง</h3><div class="formgrid"><label>ชื่อผู้รับ<input id="customer-name" autocomplete="name" placeholder="ชื่อ-นามสกุล"></label><label>โทรศัพท์<input id="customer-phone" inputmode="tel" autocomplete="tel" placeholder="08X-XXX-XXXX"></label></div><label>ที่อยู่<textarea id="customer-address" autocomplete="street-address" placeholder="บ้านเลขที่ ถนน ตำบล/แขวง"></textarea></label><div class="formgrid"><label>อำเภอ/เขต<input id="customer-district" placeholder="อำเภอ/เขต"></label><label>จังหวัด<input id="customer-province" placeholder="จังหวัด"></label></div><label>รหัสไปรษณีย์<input id="customer-postal" inputmode="numeric" autocomplete="postal-code" placeholder="XXXXX" maxlength="5"></label><label class="consent"><input type="checkbox" id="marketing-consent"> <span>รับข่าวสาร โปรโมชั่น และคูปองจาก KHONCHAIHERB (ไม่บังคับ)</span></label><small>ข้อมูลการจัดส่งใช้เพื่อดำเนินคำสั่งซื้อและบริการหลังการขาย</small></section><section class="box"><b class="shop-label">KHONCHAIHERB Official</b>${cart.map(x=>`<div class="mini">${visual(x)}<div><b>${esc(x.name)}</b><small>x${x.qty}</small></div><strong>${M(x.price*x.qty)}</strong></div>`).join('')}</section>${activeCoupon?`<div class="selected-coupon">${I('confirmation_number')}<div><b>${esc(activeCoupon)}</b><small>ส่วนลดประมาณ ${M(disc)} • ระบบจะตรวจสิทธิ์อีกครั้งก่อนสร้างออเดอร์</small></div></div>`:''}<section class="payment"><h3>วิธีชำระเงิน</h3><label class="selected"><input type="radio" name="pay" value="COD" checked>${I('payments')}<span><b>เก็บเงินปลายทาง (COD)</b><small>ชำระเมื่อได้รับสินค้า</small></span></label><label class="disabled"><input type="radio" name="pay" value="QR" disabled>${I('qr_code_2')}<span><b>พร้อมเพย์ / QR</b><small>จะเปิดหลังเชื่อม Payment Gateway และ Webhook</small></span></label></section><section class="summary rows"><div><span>ยอดสินค้า</span><b>${M(sub)}</b></div><div><span>ค่าจัดส่ง</span><b>${ship?M(ship):'ฟรี'}</b></div><div><span>ส่วนลดโดยประมาณ</span><b>-${M(disc)}</b></div><div class="grand"><span>ยอดโดยประมาณ</span><b>${M(total)}</b></div></section><div class="server-total-note">ยอดสุดท้ายจะคำนวณจากราคาสินค้า สต๊อก คูปอง และค่าจัดส่งบนเซิร์ฟเวอร์อีกครั้ง</div><div id="checkout-error" class="form-error"></div><button class="place" data-place>สั่งซื้อสินค้า</button></main>`,'');bind()}
async function placeOrder(){const el=id=>document.querySelector(id);const name=el('#customer-name')?.value.trim()||'',phone=el('#customer-phone')?.value.trim()||'',addressLine=el('#customer-address')?.value.trim()||'',district=el('#customer-district')?.value.trim()||'',province=el('#customer-province')?.value.trim()||'',postalCode=el('#customer-postal')?.value.trim()||'',pay=document.querySelector('input[name=pay]:checked')?.value||'COD',marketingConsent=Boolean(el('#marketing-consent')?.checked),err=el('#checkout-error');if(name.length<2||phone.replace(/\D/g,'').length<9||addressLine.length<5||district.length<2||province.length<2||!/^[0-9]{5}$/.test(postalCode)){if(err)err.textContent='กรุณากรอกชื่อ โทรศัพท์ ที่อยู่ อำเภอ/เขต จังหวัด และรหัสไปรษณีย์ให้ครบ';return}if(err)err.textContent='';const btn=document.querySelector('[data-place]');if(btn){btn.disabled=true;btn.textContent='กำลังยืนยันราคาและสร้างคำสั่งซื้อ...'}const payload={idempotencyKey:checkoutKey,customerName:name,phone,marketingConsent,address:{addressLine,district,province,postalCode},paymentMethod:pay,couponCode:activeCoupon,items:cart.map(x=>({id:x.id,qty:x.qty}))};try{const data=await api('/api/orders',{method:'POST',body:JSON.stringify(payload)});const record={orderNo:data.orderNo,total:Number(data.total||0),subtotal:Number(data.subtotal||0),discount:Number(data.discount||0),shipping:Number(data.shipping||0),paymentMethod:pay,paymentStatus:data.paymentStatus||'unpaid',status:data.status||'pending',fulfillmentStatus:data.fulfillmentStatus||'pending',createdAt:new Date().toISOString(),phone,items:cart.map(x=>({id:x.id,name:x.name,qty:x.qty,price:x.price}))};ordersLocal=ordersLocal.filter(o=>o.orderNo!==record.orderNo);ordersLocal.unshift(record);saveOrders();cart=[];save();checkoutKey='';success(record)}catch(e){if(err)err.textContent=orderErrorMessage(e);if(btn){btn.disabled=false;btn.textContent='สั่งซื้อสินค้า'}}}
function orderErrorMessage(e){const d=e.data||{};if(d.error==='insufficient_stock')return `สินค้าในสต๊อกไม่เพียงพอ${d.available!=null?` (เหลือ ${d.available})`:''}`;if(d.error==='product_unavailable')return 'มีสินค้าบางรายการไม่พร้อมจำหน่าย กรุณากลับไปตรวจตะกร้า';if(d.error==='coupon_not_eligible')return 'คูปองนี้ไม่สามารถใช้กับคำสั่งซื้อนี้ได้';if(d.error==='customer_details_required')return 'ข้อมูลผู้รับไม่ครบถ้วน';return 'ยังสร้างคำสั่งซื้อไม่ได้ กรุณาลองอีกครั้ง'}
function success(order){app.innerHTML=chrome(`<div class="success"><div class="successmark">${I('check')}</div><h1>สั่งซื้อสำเร็จ</h1><p>เลขที่คำสั่งซื้อ <b>${esc(order.orderNo)}</b></p><div class="successbox"><span>ยอดที่ต้องชำระ</span><b>${M(order.total)}</b><small>${order.paymentMethod==='COD'?'ชำระเงินเมื่อได้รับสินค้า':'รอยืนยันการชำระเงิน'}</small></div><div class="receipt-note">${I('receipt_long')} ใบเสร็จจะพร้อมเมื่อชำระสำเร็จ หรือเมื่อระบบยืนยันว่าเก็บเงิน COD สำเร็จแล้ว</div><div class="success-actions"><button class="primary" data-go="orders">ติดตามคำสั่งซื้อ</button><button class="secondary" data-go="home">กลับหน้าร้าน</button></div></div>`,'');bind()}
function statusText(o){const f=o.fulfillmentStatus||o.fulfillment_status||o.status;if(f==='delivered')return 'จัดส่งสำเร็จ';if(f==='shipping'||f==='shipped')return 'กำลังจัดส่ง';if(f==='packing')return 'กำลังแพ็ก';if(o.status==='cancelled')return 'ยกเลิกแล้ว';return 'รับคำสั่งซื้อแล้ว'}
