{
  "name": "khonchaiherb-commerce",
  "version": "0.3.2",
  "private": true,
  "description": "Mobile-first social commerce storefront for KHONCHAIHERB, prepared for Cloudflare Pages + D1",
  "scripts": {
    "serve": "python3 -m http.server 4173 -d public",
    "check": "node --check public/app-core.js && node --check public/app-shop.js && node --check public/app-account.js && node --check public/app-admin-actions.js"
  }
}
